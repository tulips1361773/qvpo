# reward/train (黄色实线)：一直在上升，最高达到了 30-40 分。这意味着 Agent 确实 找到了一条能拿高分的路径。
# reward/eval_mean (左下图)：一直在 -40 到 -60 之间震荡（这是你的边界惩罚 -50 分所在的范围）。这意味着 Agent 在评估时撞墙了。

# 根本原因分析：动作截断（Action Clipping）不一致
# 在强化学习处理连续动作空间（Box(-1, 1)）时，有一个巨大的陷阱：
# 你的环境 (myenv.py)：直接使用传入的动作值，没有在内部做截断。例如，如果传入 action=[2.0, 0.0, 0.0]，环境会按照 2.0 * pi 去计算角度。
# 训练阶段 (之前的代码)：PPO 从高斯分布采样：
# 由于没有截断，Agent 可能会采样出 1.5 或 -2.0 这样的值。在你的环境中，这可能代表"更极端的转向"或"更快的速度"，帮助 Agent 避开了惩罚。
# 评估阶段 (之前的代码)：我们显式加了 np.clip(action, -1, 1)。这意味着，Agent 以为输出 2.0 才能救命，但在评估时动作被强行切回 1.0，导致转向不足或速度不够，直接撞墙（获得 -50 分）。
# 这就是为什么训练分很高（用非法的超大动作避开了障碍），但评估分很低（动作被限制在合法范围内，避不开障碍）。

# 🛠️ 修改方案
# 我们需要做三件事来修复这个问题：
# 强制训练截断：在训练循环中，送入环境之前，必须把动作截断到 [-1, 1]。让 Agent 学会在合法动作范围内生存。
# 降低初始探索噪声：目前的 LogStd 默认为 0 (即标准差为 1.0)。对于范围只有 [-1, 1] 的动作，这个噪声太大了，导致 Agent 前期完全是在乱撞。我们将初始 LogStd 设为 -0.5 或 -1.0。
# 增加 Actor 确定性：在 Actor 的最后一层添加 nn.Tanh()，虽然 PPO 标准实现不强制用 Tanh，但在这种强约束环境下，Tanh 能帮助均值稳定在合法区间内。